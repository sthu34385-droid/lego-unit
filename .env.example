# Copy this file to .env.local and set strong values before deploying.
ADMIN_PASSWORD=change-me
ADMIN_SECRET=generate-a-long-random-string

# Optional on hosts with a persistent volume
# DATA_DIR=/data
# UPLOAD_DIR=/data/uploads
